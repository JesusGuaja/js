const mensaje = document.getElementById('mensaje');
const comentario = document.getElementById('comentario');
var maximo = 140;

comentario.addEventListener('input',actualizar);
function actualizar() {
    if (comentario.nodeValue.length>) {}
}
